name: Update Rule Files

on:
  push:
    paths:
      - 'Talkatone.sgmodule'  # 仅当 Talkatone.sgmodule 文件发生推送时触发工作流
  workflow_dispatch:  # 支持手动触发
    inputs:
      commit_message:
        description: 'Commit message for updates'
        required: true
        default: 'Update rule files from Talkatone.sgmodule'

jobs:
  update-rules:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python environment
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        pip install pyyaml

    - name: Update list files from Talkatone.sgmodule
      run: |
        python update_rule_files.py  # 运行自定义脚本

    - name: Commit and push updated files
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add .  # 添加所有更改
        git commit -m "${{ github.event.inputs.commit_message }}" || echo "No changes to commit"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
